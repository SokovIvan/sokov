using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Smog : MonoBehaviour
{
   /* public GameObject snog;
    // Start is called before the first frame update
    void Update()
    {

        if (Input.anyKey)
        {
        float x = Camera.main.transform.position.x-25;
        float y = Camera.main.transform.position.y+25;
        while (x <= Camera.main.transform.position.x + 25)
        {
            y = Camera.main.transform.position.y+25;
            x+=7;
            while (y>= Camera.main.transform.position.y - 25)
            {
                y-=7;
                Instantiate(snog,new Vector2(x,y),Quaternion.identity);
            }
        }
        }

    }*/
}
